# Security Access Inventory

This inventory captures the intentional broad authenticated profile-read seam for the top-level `users` collection and the now-scoped join/discovery read surface for `teams`. Phase 1 first isolated those reads behind explicit contracts, then moved member-management candidate discovery to a backend callable, and then narrowed `teams` rules once the query surface was reduced.

## Broad Collection Read Surface

| Feature / flow | File(s) | Current query shape | Required vs incidental | Candidate narrowing strategy |
| --- | --- | --- | --- | --- |
| Explicit user directory seam | `src/app/core/services/team-directory.service.ts` | `getDocs(collection(db, 'users'))` | Required today. This is the explicit, intentional broad profile-visibility seam for user-directory style flows. | Keep this seam stable unless the product makes a new decision to narrow authenticated profile visibility and update the matching UI/service contracts. |
| Explicit team directory seam | `src/app/core/services/team-directory.service.ts` | `snapObservable<Team>(query(collection(db, 'teams')))` | Required today. This is now the explicit broad-read seam for team-directory style flows. | Split join/discovery flows onto a narrower listing surface, such as public/joinable team docs or membership-scoped queries. |
| Compatibility surface: `TeamService.getAllUsers()` | `src/app/core/services/team.service.ts` | Delegates to `TeamDirectoryService.getDirectoryUsers()` | Intentional compatibility shim over the same broad profile-visibility contract. No current feature callers remain in this worktree; kept only so older call sites keep working until removed. | Remove after any out-of-band callers are migrated or deleted. |
| Compatibility surface: `TeamService.getAllTeams()` | `src/app/core/services/team.service.ts` | Delegates to `TeamDirectoryService.getDirectoryTeams()` | Incidental. No current feature callers remain in this worktree; kept only as an explicit temporary shim. | Remove after any out-of-band callers are migrated or deleted. |
| Server-backed member candidate search | `functions/team/getMembershipCandidates.js`, `src/app/core/services/team-directory.service.ts`, `src/app/features/teams/add-member-dialog/add-member-dialog.component.ts`, `src/app/features/teams/manage-team-dialog/manage-team-dialog.component.ts`, `src/app/features/teams/team-settings/team-settings.component.ts` | Backend callable `getTeamMembershipCandidates({ teamId, search? })`, exposed in Angular through `TeamDirectoryService.getMembershipCandidates(teamId, search?)` | Required today. This is the active least-privilege contract for member-management candidate discovery and no longer depends on `getDocs(collection(db, 'users'))` in the browser. | Keep this as the stable boundary for member pickers; future profile-visibility changes would need a new product decision, not just a rules tweak. |
| Join-team flow in teams workspace | `src/app/features/teams/teams/teams.component.ts` | Subscribes to `TeamDirectoryService.getDirectoryTeams()` only while the user has no team, then derives joinable results via `filterJoinableTeams(...)` | Required today for the no-team browse-and-join UX. Joined users no longer keep the full directory subscription alive. | Introduce a joinable-team listing surface with explicit visibility rules instead of all signed-in users seeing every team doc. |
| Join-team flow in settings workspace | `src/app/features/settings/settings/settings.component.ts` | Subscribes to `TeamDirectoryService.getDirectoryTeams()` only while the user has no team, then derives available teams via `filterJoinableTeams(...)` | Required today for the no-team settings-based join flow. Joined users now load only their current team doc and ceremony config. | Reuse or replace this helper with a narrowed joinable-team source once rules are tightened. |

## Notes

- Current Firestore rules intentionally allow signed-in reads of `users/{uid}` broadly, while `teams/{teamId}` reads are limited to team members, no-team discovery users, and elevated roles.
- Code search in the current worktree found the only full-collection reads at the data-access layer in the explicit directory seam and the temporary `TeamService` compatibility methods. The remaining entries above are feature-level callers that now depend directly on `TeamDirectoryService` or its shared pure helpers.
- `TeamService.getTeamsForUser(...)`, `getMembersByIds(...)`, and per-document `getTeam(...)` / `getTeamMembers(...)` are not counted as broad reads here because they already use narrower queries or direct document reads.
- Broad `teams` reads are now understood as a join/discovery concern, not a requirement for already-joined route surfaces. The current settings and teams screens now unsubscribe from the full directory once a user already belongs to a team, and the narrowed rule contract has been emulator-verified against both direct reads and live query shapes.
- Member-management candidate pickers remain intentionally separated from the broad `users` profile-read seam: add-member, manage-team, and team-settings depend on the backend candidate-search contract instead of `getDocs(collection(db, 'users'))` in the browser.
- The remaining `users` profile-visibility seam is the explicit directory seam (`TeamDirectoryService.getDirectoryUsers()`), the temporary compatibility shim (`TeamService.getAllUsers()`), and any out-of-band callers that may still expect broad visibility.
- The candidate-search rollout assumes legacy user docs have `teamId` present; environments that still contain missing-field docs should run `functions/team/backfillMissingUserTeamIds.js` before switching member pickers to the callable-backed path.
- Team membership writes no longer block `users/{uid}` rule tightening directly: the active add/join/remove/leave flows now route through the privileged `updateTeamMembership` callable in `functions/team/updateMembership.js` instead of writing another user's profile from the browser.
